import type { NextConfig } from 'next';

// Next.js configuration with strict defaults for production readiness.
const nextConfig: NextConfig = {
  reactStrictMode: true
};

export default nextConfig;
